using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TableThinkingGame : MonoBehaviour
{
    //matriz que contiene los objetos a mostrar
    private GameObject [,] objectImages;

    //Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void CreateTable(){

        int x = 3;
        int y = 4;

        int aux = 2;

        for (int i = 0; i < 4; i++)
        {
            for (int j = 0; j < 2; j++)
            {
                if(i == aux){

                }else{
                    
                }
            }
        }
        
    }
}
